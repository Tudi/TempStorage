#ifndef PROFILEAG_COMPARISON_H
#define PROFILEAG_COMPARISON_H

#include <profile_tag.h>

void assert_ProfileTag_equal(const struct ProfileTag* op1, const struct ProfileTag* op2);

#endif // PROFILEAG_COMPARISON_H
