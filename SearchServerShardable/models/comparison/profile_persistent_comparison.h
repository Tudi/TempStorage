#ifndef PROFILE_PERSISTENT_COMPARISON_H
#define PROFILE_PERSISTENT_COMPARISON_H

#include <profile_persistent.h>

void assert_ProfilePersistent_equal(const struct ProfilePersistent* op1,
    const struct ProfilePersistent* op2);

#endif // PROFILE_PERSISTENT_COMPARISON_H
