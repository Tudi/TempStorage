#ifndef POSITION_PERSISTENT_COMPARISON_H
#define POSITION_PERSISTENT_COMPARISON_H

#include <position_persistent.h>

void assert_PositionPersistent_equal(const struct PositionPersistent* op1,
    const struct PositionPersistent* op2);

#endif // POSITION_PERSISTENT_COMPARISON_H
