#ifndef COMPOSITE_SCORE_COMPARISON_H
#define COMPOSITE_SCORE_COMPARISON_H

#include <composite_score.h>

void assert_CompositeScore_equal(const struct CompositeScore* op1,
    const struct CompositeScore* op2);

#endif // COMPOSITE_SCORE_COMPARISON_H
